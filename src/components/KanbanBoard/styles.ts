const styles = {
    containerWrapper: {
        margin: 20,
    },
    container: {
        display: "flex",
        flexDirection: "row",
        justifyContent: "space-between",
        maxHeight: 700,
        minHeight: 500,
        width: '100%',
    }
}

export default styles;
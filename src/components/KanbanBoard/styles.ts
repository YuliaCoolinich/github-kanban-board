const styles = {
    segment: {
        display: "flex",
        flexDirection: "row",
        justifyContent: "space-between",
        maxHeight: 700,
        minHeight: 500,
    }
}

export default styles;
import { DOMAIN } from '../../../data/urls';

export const ERROR_VALIDATION_TYPES = {
    INCORRECT_DOMAIN: `Incorrect domain, use ${DOMAIN}`,
    EXTRA_SLASH: `Incorrect URL, check the number of slashes / in your URL`,
}